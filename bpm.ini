[package]
name=path
version=1.0.0
include=libpath
description=Find your way around directory trees.

[dependencies]
0=assign
1=log

[devDependencies]
0=embed-docs
1=unittest

[scripts]
readme=embed-docs README.md libpath
test=unittest test/*.test
